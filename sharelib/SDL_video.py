SDL_HITTEST_NORMAL = 0  # /**< Region is normal. No special properties. */
SDL_HITTEST_DRAGGABLE = 1  # /**< Region can drag entire window. */
SDL_HITTEST_RESIZE_TOPLEFT = (
    2  # /**< Region is the resizable top-left corner border. */
)
SDL_HITTEST_RESIZE_TOP = 3  # /**< Region is the resizable top border. */
SDL_HITTEST_RESIZE_TOPRIGHT = (
    4  # /**< Region is the resizable top-right corner border. */
)
SDL_HITTEST_RESIZE_RIGHT = 5  # /**< Region is the resizable right border. */
SDL_HITTEST_RESIZE_BOTTOMRIGHT = (
    6  # /**< Region is the resizable bottom-right corner border. */
)
SDL_HITTEST_RESIZE_BOTTOM = 7  # /**< Region is the resizable bottom border. */
SDL_HITTEST_RESIZE_BOTTOMLEFT = (
    8  # /**< Region is the resizable bottom-left corner border. */
)
SDL_HITTEST_RESIZE_LEFT = 9  # /**< Region is the resizable left border. */
